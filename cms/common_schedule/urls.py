from django.urls import path
from . import views 

urlpatterns = [
  
    path('common_sched_calendar', views.common_sched_calendar, name="common-sched-calendar"),
    path('all_events', views.all_events, name='all-events'),
    path('find_common_schedule', views.find_common_schedule, name='find_common_schedule'),
    # path('add_event', views.add_event, name='add-event'), 
    # path('update', views.update, name='update'),
    # path('remove', views.remove, name='remove'),
    
]




































































































































































































































































































































































